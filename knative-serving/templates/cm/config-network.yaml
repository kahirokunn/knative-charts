{{- if or .Values.configNetwork .Values.allowEmptyConfigMap -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: config-network
  labels:
    app.kubernetes.io/name: knative-serving
    app.kubernetes.io/component: controller
data:
  {{- toYaml .Values.configNetwork | nindent 2 }}
{{- end -}}
